# Psicologia Clínica | Nicole Mamede

Este é o site profissional da psicóloga Nicole Mamede, desenvolvido como uma Single Page Application (SPA) para apresentar seus serviços, abordagem (Análise do Comportamento e DBT) e facilitar o contato com pacientes.

## 🛠️ Tecnologias

- **React 19**: Biblioteca UI principal.
- **TypeScript**: Tipagem estática para maior robustez.
- **Vite**: Build tool rápida e moderna.
- **Tailwind CSS**: Framework de estilização utilitário.
- **Lucide React**: Ícones leves e flexíveis.

## 🚀 Como Rodar o Projeto

1. **Instale as dependências:**
   ```bash
   npm install
   ```

2. **Inicie o servidor de desenvolvimento:**
   ```bash
   npm run dev
   ```
   O projeto estará acessível em `http://localhost:3000`.

3. **Gere a build de produção:**
   ```bash
   npm run build
   ```

## 📂 Estrutura do Projeto

```
src/
├── components/   # Componentes React reutilizáveis
├── constants/    # Constantes globais (ex: textos, configurações)
├── types/        # Definições de tipos TypeScript
├── utils/        # Funções utilitárias
├── App.tsx       # Componente raiz da aplicação
├── main.tsx      # Ponto de entrada (montagem do React)
└── index.css     # Estilos globais e diretivas do Tailwind
```

## 🎨 Design System

O projeto utiliza um design system customizado via Tailwind, com foco em cores vibrantes e acolhedoras ("Spring Palette") e tipografia elegante (Inter + Playfair Display).
